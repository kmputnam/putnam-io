<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{ title }} | {{ site.name }}</title>
    <meta
      name="description"
      content="Executive profile for {{ site.name }} focused on product strategy and platform architecture."
    />
    {# Keep both root and /assets icon links to satisfy Safari + legacy favicon lookups. #}
    <meta name="theme-color" content="#1D4ED8" />
    <link rel="icon" href="/assets/icons/favicon.svg" type="image/svg+xml" />
    <link rel="icon" href="/favicon.ico" sizes="any" />
    <link rel="alternate icon" href="/assets/icons/favicon.ico" sizes="any" />
    <link rel="icon" type="image/png" sizes="32x32" href="/assets/icons/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/assets/icons/favicon-16x16.png" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/assets/icons/apple-touch-icon.png" />
    <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#1D4ED8" />
    <link rel="manifest" href="/site.webmanifest" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&family=JetBrains+Mono:wght@400&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/assets/css/styles.css" />
  </head>
  <body>
    <a class="skip-link" href="#main-content">Skip to content</a>
    <header class="site-header" aria-label="Primary">
      <div class="container header-inner">
        <a class="header-home{% if page.url == '/' %} is-active{% endif %}" href="/">Home</a>
        <nav class="site-nav" aria-label="Primary navigation">
          {% for item in site.nav %}
            {# Keep parent sections active for nested routes like /writing/some-post/. #}
            {% set isActive = page.url == item.href or (page.url and item.href != '/' and page.url.indexOf(item.href) == 0) %}
            {# aria-current drives accessibility and the visual "current page" indicator. #}
            <a
              class="{% if isActive %}is-active{% endif %}"
              href="{{ item.href }}"
              {% if isActive %}aria-current="page"{% endif %}
            >{{ item.label }}</a>
          {% endfor %}
        </nav>
      </div>
    </header>

    <main id="main-content" class="container main-content">{{ content | safe }}</main>
  </body>
</html>
